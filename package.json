{
  "name": "lambda-warmer",
  "version": "1.0.0-alpha.0",
  "description": "Lambda that warms other lambdas",
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf dist",
    "rebuild": "npm run clean && npm install && npm run build",
    "test": "mocha $NODE_DEBUG_OPTION",
    "build": "tsc",
    "deploy": "node deploy.js"
  },
  "author": "Jon Rapp",
  "license": "UNLICENSED",
  "dependencies": {
    "aws-sdk": "2.604.0",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@types/chai": "4.1.7",
    "@types/mocha": "5.2.7",
    "@types/node": "10.17.13",
    "chai": "4.2.0",
    "execa": "^4.1.0",
    "mocha": "6.1.4",
    "source-map-support": "0.5.13",
    "ts-node": "8.3.0",
    "typescript": "3.7.4"
  }
}
